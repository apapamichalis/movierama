#sorts
   Sort by (most):
   = link_to 'Recent', movies_path(:sort => 'date_added')
   = " | "
   = link_to 'Likes', movies_path(:sort => 'likes')
   = " | "
   = link_to 'Hates', movies_path(:sort => 'hates')
   = " || "
   = link_to 'Reset', movies_path
#movies-list
   - @movies.each do |movie|
      = render 'movie_partial', :movie => movie

= link_to 'New Movie', new_movie_path
